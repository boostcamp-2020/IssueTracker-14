import styled from "styled-components";

const NewCommentFormWrapper = styled.div`
  display: flex;
  margin: 0 1rem;
`;

const NewCommentForm = styled.div`
  width: 100%;
`;

const ButtonWrapper = styled.div`
  margin-top: 1rem;
  display: flex;
  justify-content: flex-end;
`;

export default { NewCommentFormWrapper, NewCommentForm, ButtonWrapper };
